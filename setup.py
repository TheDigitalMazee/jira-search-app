from setuptools import setup

setup(
    name="jira-search-app",
    version="0.1",
    install_requires=[
        "streamlit>=1.28.0",
        "sentence-transformers>=2.2.0",
        # other dependencies
    ],
)
